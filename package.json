{
  "name": "kantelbeer-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check:links": "node scripts/check-links.mjs",
    "ci": "npm run typecheck && npm test && npm run build && npm run check:links",
    "preview": "astro preview",
    "typecheck": "astro check",
    "test": "vitest run",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.2.10"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^26.4.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11"
  },
  "allowScripts": {
    "esbuild": true
  }
}
